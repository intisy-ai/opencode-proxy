{
  "name": "opencode-proxy",
  "version": "0.1.0",
  "description": "The OpenCode proxy layer (opencodeProfile) on top of the generic core-proxy engine",
  "type": "module",
  "main": "dist/index.js",
  "license": "MIT",
  "author": "intisy",
  "repository": { "type": "git", "url": "git+https://github.com/intisy-ai/opencode-proxy.git" },
  "keywords": ["proxy", "routing", "anthropic", "opencode", "intisy", "opencode-proxy"],
  "files": ["dist/", "README.md", "LICENSE"],
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "build": "npm --prefix core-proxy/core-ir install && npm --prefix core-proxy/core-ir run build && npm --prefix core-ir install && npm --prefix core-ir run build && npx tsc --project core-proxy/tsconfig.json --types node && npx tsc",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": { "typescript": "^5.6.0", "vitest": "^2.1.0", "@types/node": "^22.0.0", "esbuild": "^0.21.5" }
}
